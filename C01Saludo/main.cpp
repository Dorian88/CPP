#include <iostream> //Directiva: Son instrucciones que el preprocesador de C++ interpreta y ejecuta antes de que el código sea compilado por el compilador.

using namespace std; //Namespace es una forma de organizar el código, evitando conflictos de nombres entre diferentes partes del proyecto.
                     //namespace std es un espacio de nombres que contiene todos los elementos de la biblioteca estándar de C++
//Esto es una funcion
int main(){

    cout << "Hola Dorian! Que haces?" << endl; //Esto es una sentencia y termina con un punto y coma
    return 0;
}
